<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yangj.en_0617">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EIntroActivity" />
        <activity android:name=".EJoinProtectorActivity" />
        <activity android:name=".EJoinUserActivity" />
        <activity android:name=".ELoginActivity" />
        <activity android:name=".EChatActivity" />
        <activity android:name=".EJoinActivity" />
        <activity android:name=".LocationActivity" />
        <activity android:name=".EStartActionActivity"/>
        <activity android:name=".MapViewActivity"></activity>
    </application>

</manifest>